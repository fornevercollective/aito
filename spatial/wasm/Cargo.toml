[package]
name = "booth-modulator"
version = "0.1.0"
edition = "2021"
description = "WASM modulation curves for gsplat booth (repel / kbatch hook pattern)"

[lib]
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = "s"
lto = true
strip = true