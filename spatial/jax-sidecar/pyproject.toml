[project]
name = "aito-booth-jax"
version = "0.1.0"
description = "Optional JAX depth-lift sidecar for aito-mac gsplat booth"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
jax = ["jax[cpu]>=0.4.0", "jaxlib>=0.4.0", "numpy>=1.26.0", "pillow>=10.0.0"]

[project.scripts]
aito-booth-jax = "booth_jax:main"